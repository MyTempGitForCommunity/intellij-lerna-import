{
  "name": "test",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "lerna run build",
    "clean:dist": "lerna run clean",
    "bootstrap": "lerna bootstrap",
    "publish": "lerna publish"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
